from django.shortcuts import renderfrom django.contrib.auth.decorators import login_requiredfrom .models import Payment@login_requireddef payment_history_view(request):    """Fetch and display payment history for the logged-in user."""        payments = Payment.objects.filter(user=request.user).order_by('-date')  # Fetch payment history    return render(request, "payments/payment_history.html", {        "payments": payments    })